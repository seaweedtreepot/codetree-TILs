#include <iostream>
#include <string>
using namespace std;
int main() {
    // 여기에 코드를 작성해주세요.
    string str1; str2;
    cin >> str1 >> str2;

    if(str1.length>str2.length()){
        cout << str1 ;
    }else if (str1.length()==str2.length()){
        cout << "same";
    }else{
        cout << str2;
    }
    return 0;
}